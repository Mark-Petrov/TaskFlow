# Git
.git
.gitignore

# Dependencies (кроме сгенерированного Prisma client — см. ! ниже)
node_modules
server/node_modules
!server/node_modules/.prisma
!server/node_modules/.prisma/**

# Исходники не нужны в образе — только dist
src
server/src
server/tsconfig.json

# Старый корневой dist (vite legacy)
dist

# Local env & secrets
.env
.env.local
.env.*.local
server/.env

# SQLite dev database
server/prisma/dev.db
server/prisma/dev.db-journal
server/prisma/*.db
server/prisma/*.db-journal

# Editor & OS
.vscode
.idea
.DS_Store
*.log

# Docker
docker-compose.override.yml

# Docs / misc
agent-transcripts
assets

# frontend/dist и server/dist НЕ игнорируем — они копируются в образ
